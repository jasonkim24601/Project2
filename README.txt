Provided example URLs are in the comments of main.py
Be sure that cardnames.csv is in the directory or else the program will not run.
The resulting .csv files sometimes had a graphical error when I opened it in Excel.
Visually it might look as if some rows are empty but there is content in them, just click to double check.
